Algorithms
 
1. Write an algorithm to find the product of first n odd numbers
solution: Step1: start
          Step2: read n odd numbers
          Step3: initialize mul=1,i=1 and count<---1
          Step4: repeat the steps (i)-(iii) until(count<=number)
                 (i)mul<---mul*i
                 (ii)i<---i+2
                 (iii)count<---count+1
         Step5: print "mul of ood numbers"
         Step6: end

2. Write an algorithm to find the count of positive  factors of an integer
solution : Step1: start
           Step2: read thepositive integer n
           Step3: initialize i<---1,sum<---0
           Step4: repeat the steps (i)-(ii) until(i<=n)
                  (i) if(n%==0)
                      sum<---sum+1
                  (ii) i<---i+1
          Step5: print "count"
          Step6: end

3.Write an algorithm to find the number of positive factors of an integer
solution : Step1: start      
           Step2: read the positive integer n
           Step3: intialize i<---1,sum<---0
           Step4: repeat the steps (i)-(ii) until(i<==n)
                  (i) if(n%i==0)
                      sum<---sum+1
                  (ii) i<---i+1
           Step5: print "sum"
           Step6: end